[project]
name = "dnsearcher"
version = "0.1.0"
description = "From DNS records to DNS intelligence."
requires-python = ">=3.11"
dependencies = ["dnspython>=2.6", "pyyaml>=6", "rich>=13", "httpx>=0.27"]

[project.scripts]
dnsearcher = "dnsearcher.cli:main"